# V1.5.6

- In Sawt Aljazair, public publishing is represented by Reports/بلاغات, not a separate Posts feature.
- Removed the separate منشور action from Flutter UI.
- Home feed now loads /reports and displays submitted reports.
- Profile update uses a transaction for User + SawtAljazairProfile and returns a clear server error.
- No database reset/drop.
